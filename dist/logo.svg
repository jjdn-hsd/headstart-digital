<?xml version="1.0" encoding="UTF-8"?>
<svg width="600" height="200" viewBox="0 0 600 200" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Orbital Path -->
  <path d="M50 100C50 50 150 20 250 50" stroke="#00A3FF" stroke-width="4" stroke-linecap="round" fill="none" opacity="0.3"/>
  
  <!-- Rocket Body -->
  <g transform="translate(200, 60) rotate(45)">
    <!-- Main Rocket Shape -->
    <path d="M0 0L80 80L40 120L0 80L0 0Z" fill="#FF6B21"/>
    <path d="M0 0L-40 40L0 80L0 0Z" fill="#FF8C42"/>
    
    <!-- Flame Accent -->
    <path d="M40 120L20 140L0 120L40 120Z" fill="#FF1E1E"/>
    
    <!-- Window/Cockpit -->
    <circle cx="30" cy="30" r="15" fill="#FFFFFF"/>
    <circle cx="30" cy="30" r="12" fill="#00A3FF"/>
  </g>

  <!-- Company Name -->
  <g transform="translate(320, 100)">
    <!-- HeadStart -->
    <text x="0" y="0" font-family="Arial, sans-serif" font-size="48" font-weight="700" fill="#1A1A1A">
      HeadStart
    </text>
    <!-- Digital -->
    <text x="0" y="50" font-family="Arial, sans-serif" font-size="42" font-weight="600" fill="#404040">
      Digital
    </text>
  </g>

  <!-- Decorative Elements -->
  <g stroke="#00A3FF" stroke-width="2" opacity="0.2">
    <line x1="320" y1="160" x2="580" y2="160"/>
    <line x1="320" y1="163" x2="580" y2="163"/>
  </g>

  <!-- Ensure SVG is scalable -->
  <defs>
    <style>
      @media (max-width: 300px) {
        text { transform: scale(0.8); }
      }
    </style>
  </defs>
</svg>